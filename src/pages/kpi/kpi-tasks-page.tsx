import { KpiTasksList } from '@/features/kpi/tasks/ui/kpi-tasks-list'

export default function KpiTasksPage() {
  return (
    <div className="container mx-auto flex h-full flex-col overflow-hidden">
      <KpiTasksList />
    </div>
  )
}
