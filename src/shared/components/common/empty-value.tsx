/**
 * Placeholder for a field the record simply does not carry.
 *
 * It used to be red almost everywhere, which reads as "something is wrong"
 * rather than "there is nothing here" - on a detail page full of them that
 * turns an ordinary record into a page of warnings. Muted keeps it legible
 * while letting the values that are present hold the eye.
 */
export const EmptyValue = () => <span className="text-neutral-450 font-normal">Mavjud emas</span>
